{
  "name": "kaolaqi-skills",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "skills/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "install-skills": "node install-skills.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
